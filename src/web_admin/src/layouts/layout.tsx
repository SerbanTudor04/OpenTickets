export default function Layout(props:any){
    
    return (
        <>
          <section>
          <div className=" ">
            {props.children}
            </div>

          </section>

            
 
        </>
      )
}